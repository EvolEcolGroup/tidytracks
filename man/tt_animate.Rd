% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tt_animate.R
\name{tt_animate}
\alias{tt_animate}
\title{Animate tracks over time}
\usage{
tt_animate(
  x,
  type = c("points", "paths"),
  basemap = NULL,
  wake_length = 0.5,
  path_alpha = 0.5,
  plot_lims = NULL,
  label_format = "\%Y-\%m-\%d \%H:\%M:\%S"
)
}
\arguments{
\item{x}{A \code{move2} object with a defined CRS.}

\item{type}{Either points or paths. Points = a moving animal with a fading
memory; paths = a growing trajectory.}

\item{basemap}{An sf object to use as a basemap for the animation. If \code{NULL},
no basemap is used.}

\item{wake_length}{Numeric, length of the wake behind each point as a
proportion of the total length of the animation (only for \code{type = "points"}).}

\item{path_alpha}{Numeric, the alpha to use for the path (only for
\code{type ="path"}).}

\item{plot_lims}{A numeric vector of length 4 giving the limits of the plot
(\code{xmin}, \code{ymin}, \code{xmax}, \code{ymax}), default is \code{NULL} which uses the full
extent of the data, but you may wish to add a buffer around the bounding
box of \code{x}.}

\item{label_format}{A character string specifying the format of the date-time
labels on the animation frames. Default is \code{"\%Y-\%m-\%d \%H:\%M:\%S"}.}
}
\value{
A \code{gganimate} object. To save this as a gif or video, use the
function \code{anim_save()} from the \code{gganimate} package.
}
\description{
This function takes a tidytracks object (with a CRS defined) and creates an
animated visualization of the tracks over time using ggplot2 and gganimate.
Currently implemented only to: use the CRS defined in the tidytracks object,
plot on a supplied basemap, and have one frame per time step already in the
events data. So before running this function, you need to make sure your data
are in the correct CRS, your basemap is in the same CRS, and the time
interval between each event is regular and appropriate for you desired
animation. Points are automatically coloured by the field in the \code{move2}
object used as the track ID (e.g. \code{track_id}).
}
\details{
TODO we may need to add more parameters e.g. for point size, colour palette,
etc. AND implement with different basemap options (e.g. terra raster). NB.
Timestamps must be regular AND the same (e.g. 13:00, 13:10, 13:20) for all
individuals so that we can have one frame per datetime - the
\code{move2::mt_interpolated()} function can be used to do this.

The options for \code{type} are very important:
\itemize{
\item \code{points}: each frame shows the current location of each animal as a point,
with a fading "wake" behind it to show recent movement. The locations need to
be frequent enough that a trail of points looks like a trailing path, at your
spatial scale.
\item \code{paths}: each frame shows the current point, and full trajectory of each
animal up to that time which does not fade.
}
}
