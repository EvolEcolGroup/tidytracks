% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/animate_map.R
\name{animate_map}
\alias{animate_map}
\title{Animate a user-built map with track paths or points}
\usage{
animate_map(
  p,
  x,
  fade = TRUE,
  wake_length = 0.5,
  label_format = "\%Y-\%m-\%d \%H:\%M:\%S"
)
}
\arguments{
\item{p}{A \code{ggplot} object containing the fully styled map, with the
\code{move2} tracking layer added using \code{geom_event_path} or
\code{geom_event_point}.}

\item{x}{The same \code{move2} object as used in the plot. Used to identify the
datetime column name and compute the number of animation frames.}

\item{fade}{Logical. Only relevant for path animations. If \code{TRUE}
(default), previous segments fade out behind the current one using
\code{shadow_wake_build}. If \code{FALSE}, all previous segments
remain fully visible, producing a permanently growing path.}

\item{wake_length}{Numeric between 0 (exclusive) and 1 (inclusive). Length
of the fading trail as a proportion of the total animation length. Only
used when \code{fade = TRUE} (paths) or for point animations. Default is
\code{0.5}.}

\item{label_format}{A character string specifying the
\code{\link[base]{format.POSIXct}} format for the date-time label shown on
each frame. Default is \code{"\\\%Y-\\\%m-\\\%d \\\%H:\\\%M:\\\%S"}.}
}
\value{
A named list with: \code{p_anim} (a \code{gganim} object ready to pass to
\code{gganimate::animate()}) and \code{n_timesteps} (the number of unique time steps
in the layer data).
}
\description{
Takes a \code{ggplot} object built by the user, and the \code{tidytracks} tracking
layer plotted, and adds \code{gganimate} animation so that track data moves
through time.
}
\details{
The user first builds a complete map with all desired layers (e.g. raster
basemap, polygon overlays, track data, themes, limits) and gets it looking
exactly right as a static plot. They then pipe it into this function to add
animation. Layers whose data do not contain the \verb{track datetime} column (such
as a bathymetry raster or land polygons) are automatically kept static across
all frames by gganimate.

The track data layer must be added to the \code{ggplot} with either
\code{\link{geom_event_path}} or \code{\link{geom_event_point}}. The function
detects which is present from the geometry type in the layer data and applies
the appropriate animation:

\itemize{
\item{ \strong{Path layer} (\code{geom_event_path}): if \code{fade = TRUE},
segments fade out behind the current one; if \code{fade = FALSE}, all
previous segments remain visible, producing a permanently growing path.}
\item{ \strong{Point layer} (\code{geom_event_point}): points move through
time with a fading, shrinking wake regardless of \code{fade}.}
}

All aesthetic choices (colour, size, linewidth, alpha, etc.) are set by the
user in their \code{geom_event_path()} or \code{geom_event_point()} call.
}
