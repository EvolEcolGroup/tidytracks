% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_event_column.R
\name{as_event_column}
\alias{as_event_column}
\title{Move one (or more) variable(s) from the metadata to the event data as a new
column(s).}
\usage{
as_event_column(x, ..., .keep = FALSE)
}
\arguments{
\item{x}{A \code{move2} object}

\item{...}{One or more unquoted column names from the metadata to move to the
event data; it is possible to use tidyselect helpers to select multiple
columns.}

\item{.keep}{A logical value indicating whether to keep the original metadata
columns in the metadata after moving them to the event data. The default is
\code{FALSE}, which means that the original metadata columns will be removed.}
}
\value{
A \code{move2} object with the specified metadata columns added to the
event data.
}
\description{
This function moves one or more columns from the metadata of a \code{move2} object
to the event data, adding them as new columns. This is useful when you want
to include metadata information in the event-level analysis.
}
\examples{
example_tt2 <- as_event_column(example_tt, sex)
example_tt2 # now showing sex in the events table
}
