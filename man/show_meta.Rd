% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/show_meta.R
\name{show_meta}
\alias{show_meta}
\alias{show_meta<-}
\title{Show or set the track metadata of a \code{move2} object}
\usage{
show_meta(x)

show_meta(x) <- value
}
\arguments{
\item{x}{A move2 object}

\item{value}{A data frame with metadata to set for the \code{move2} object}
}
\value{
The metadata table from the input \code{move2} object (nothing is
returned if using the setter function).
}
\description{
The track metadata is stored as a data frame within the \code{move2} object. This
function allows you to view or modify this metadata. When retrieving the
metadata, it returns a tibble containing the metadata for each track. When
setting the metadata, you can provide a new data frame with the updated
metadata, or modify or remove specific columns, as you would normally do with
a data.frame/tibble. If providing a new metadata table, make sure that
the number of rows matches the number of tracks in the \code{move2} object, and
that the track IDs correspond to those in the \code{move2} object.
}
\examples{
show_meta(example_tt)
show_meta(example_tt)$age <- c(2, 3, 2)
show_meta(example_tt)
}
