% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event_interval.R
\name{event_interval}
\alias{event_interval}
\title{Measure the time interval between pairs of consecutive events within each
track}
\usage{
event_interval(x, units)
}
\arguments{
\item{x}{A move2 object}

\item{units}{Optional, the time units (as \code{character}, \code{symbolic_units} or
\code{units}) used to represent the intervals. It defaults to the units of the
input data.}
}
\value{
a vector of time intervals of the same length as the number of
events in \code{x}, with the last value set to NA for each track.
}
\description{
This function measures the interval (i.e. time interval) between adjacent
pairs of consecutive events within each track. It returns a vector of
intervals of the same length as the number of events in \code{x}, with the value
for the last event of each track padded with an NA.
}
\details{
This is a wrapper around \code{move2::mt_time_lags()}. Note that the timestamps of
events have to be ordered for this function to work correctly (you can use
\code{\link[=tt_order_time]{tt_order_time()}} to order your tibble of tracks.
}
\examples{
event_interval(example_tt)
event_interval(example_tt, units = "hours")

}
