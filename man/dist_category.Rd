% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist_category.R
\name{dist_category}
\alias{dist_category}
\title{Create a categorical distance matrix}
\usage{
dist_category(x)
}
\arguments{
\item{x}{A factor vector.}
}
\value{
A numeric matrix with one row and one column for each element of \code{x}.
If \code{x} has names, they are used as row and column names.
}
\description{
\code{dist_category()} takes a factor vector and returns a pairwise distance
matrix indicating whether observations belong to different categories.
}
\details{
The returned matrix contains:
\itemize{
\item \code{0} when two observations are in the same category
\item \code{1} when two observations are in different categories
\item \code{NA} when either observation has a missing value
}
}
\examples{
group <- factor(c("A", "A", "B", "C"))

dist_category(group)

# Named input preserves names in the output matrix
named_group <- factor(c("red", "blue", "red"))
names(named_group) <- c("sample1", "sample2", "sample3")

dist_category(named_group)

# Missing values return NA for comparisons involving the missing value
group_with_na <- factor(c("A", NA, "B"))
dist_category(group_with_na)

}
\seealso{
\code{\link[=same_category]{same_category()}}
}
